{
  "builds": [{ "src": "src/**/*.ts", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "src/app.ts" }],
  "functions": {
    "api": {
      "maxDuration": 10
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}
